// export const handleAddToCart = (jobDetails) => {
    
   
//     const exists = jobs.find((pd) => pd.id === jobDetails.id);
 
//    return exists;
    
  
//   };

//   export default handleAddToCart;